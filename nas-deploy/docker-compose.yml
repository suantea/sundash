version: '3.8'

services:
  sundash:
    image: sundash:latest
    container_name: sundash
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - SUNDASH_PORT=3000
      - SUNDASH_DATA_DIR=/app/data
      - TZ=Asia/Shanghai
